{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/bitsyc",
            "args": [
                "${workspaceFolder}/../bitsyspec/specs/primes.bitsy"
            ],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
