{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/bitsyc",
            "args": [
                "${workspaceFolder}/examples/simple.bitsy"
            ],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
